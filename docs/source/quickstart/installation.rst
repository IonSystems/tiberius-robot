Installation
============

The `tiberius` package can be fully installed by running the setup script (`setup.py`), found in the top level folder.
The `setup.py` script attempts to install all necessary dependencies for the `tiberius` package.
