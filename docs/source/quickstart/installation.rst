Installation
====================
The tiberius-robot package can be fully installed by running the setup script, found in the top level folder.
The setup.py script attempts to install all necessary dependencies for tiberius-robot.
